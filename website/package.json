{
  "name": "ckman-website",
  "version": "0.0.1",
  "private": true,
  "description": "CKMAN documentation site (VitePress)",
  "scripts": {
    "docs:dev": "vitepress dev --host 0.0.0.0",
    "predocs:build": "node scripts/fetch-latest-release.mjs",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "dependencies": {
    "marked": "^18.0.4"
  }
}
